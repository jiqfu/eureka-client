server:
  # 启动多次 记得换端口
  port: 8762

spring:
  application:
    name: eureka-client

eureka:
  client:
    serviceUrl:
      # 注册中心地址
      defaultZone: http://localhost:8761/eureka/,http://localhost:8760/eureka/
